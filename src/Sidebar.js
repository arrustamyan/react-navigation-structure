import React from 'react';

export default function Sidebar({ navigation: Navigation }) {
    return (
        <side>
            <div>here goes logo</div>
            { Navigation }
        </side>
    );
}
